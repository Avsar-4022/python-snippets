"""
 Program to compute the remainder when two numbers are divides
 using the % operator
"""
num1 = float(input("Enter the dividend: "))
num2 = float(input("Enter the divisor: "))
remainder = num1 % num2
print(f"The remainder is {remainder}\n")
